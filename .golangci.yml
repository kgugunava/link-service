run:
  timeout: 3m
  tests: true

linters:
  enable:
    - goimports      
    - govet          
    - staticcheck   
    - errcheck    
    - unused         

linters-settings:
  goimports:
    local-prefixes: github.com/kgugunava/link-service

issues:
  exclude-use-default: false